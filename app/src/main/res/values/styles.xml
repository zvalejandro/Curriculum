<resources>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light"/>

    <style name="CVTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CVTheme.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>

        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:windowDisablePreview">true</item>
    </style>


    <!-- STYLES IN ORDER TO APPLY IN TEXT VIEW -->
    <style name="TextTitle" parent="android:Widget.TextView">
        <item name="android:textColor">@color/grayBrown</item>
        <item name="android:textSize">@dimen/text_default_normal</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/font_cv_semi_bold</item>
    </style>

    <style name="TextSubtitle" parent="android:Widget.TextView">
        <item name="android:textColor">@color/grayBrown</item>
        <item name="android:textSize">@dimen/text_default_normal</item>
        <item name="android:fontFamily">@font/font_cv_normal</item>
    </style>

    <style name="TextDesc" parent="android:Widget.TextView">
        <item name="android:textColor">@color/grayBrown</item>
        <item name="android:textSize">@dimen/text_default_normal</item>
        <item name="android:fontFamily">@font/font_cv_light</item>
    </style>


</resources>
